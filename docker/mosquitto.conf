# Mosquitto MQTT Broker Configuration
# For EnergyPulse Energy Prediction System

# Listen on default MQTT port
listener 1883

# Allow anonymous connections (for development)
allow_anonymous true

# Enable WebSocket support (optional, for web clients)
listener 9001
protocol websockets

# Logging
log_type all
log_dest stdout
